// https://codeforces.com/problemset/problem/129/B
#include <bits/stdc++.h>

using namespace std;

template <typename T> void print_vec(vector<T> &vec) {
  for (T i : vec) {
    cout << i << " ";
  }
  cout << endl;
}

int kick_out(vector<vector<int>> &graph) {}

int main() {
  int n, m;
  cin >> n >> m;
  
  
  vector<vector<int>> graph;
  graph.reserve(n);
  int src, dst;
  
  for(int i=0; i < n; ++i) {
    vector<int> links;
    links.reserve(
    graph.push_back
  }

  
  
  
}
